/* Swapping two numbers(using XOR operator)
Example:
Input: 10 20
Output: 20 10
*/
#include <stdio.h>
int main(){
    int A, B;
    scanf("%d %d",&A,&B);
    A = A^B; // A = A^B
    B = A^B; // B = A^B = (A^B)^B = A 
    A = A^B; // A = A^B = (A^B)^A = B
    printf("%d %d",A,B);
    return 0;
}
